# 2026春计算机网络与通信课设

> 由 PDF 自动转换为 Markdown。原始分页用二级标题标出；版式、图片和表格未完整保留。

## 第 1 页

课程设计教学目标：

深入理解计算机网络五层体系结构 从物理层到应用层 理清数据在

各层的封装与解封装过程。

掌握 Socket 编程： 熟练使用 TCP 或 UDP 套接字开发网络应用，处理

并发、粘包、超时等实际问题。

初步接触 AI+ 网络： 通过调用 AI API 或训练简单模型，感受人工智

能如何赋能传统网络应用。

课设基本要求：

编程语言： Python Java 均可。

团队规模： 人 组（鼓励不同角色分工：网络编程 +AI 模块

代码规范： 提供清晰注释，重要函数有 docstring

题目一： AI 智能问答聊天工具（难度中等）

目标：

实现一个基于 TCP Socket 的 C/S 架构聊天程序 并集成一个 AI 大模

型 API （免费的有 DeepSeek 、百度文心、讯飞星火等 ，使服务器能

智能回复或辅助用户。

核心要求（必做

C/S 架构： 客户端用 Python 或 Java/C++ ，服务器用多线程 异步

处理多个客户端连接。

AI 集成 客户端发送自然语言问题 服务器接收并调用 AI API

## 第 2 页

返回答案 客户端显示 API 调用可用开源 SDK 或直接 HTTP 请求

五层原理验证报告 要求学生通过抓包 Wireshark 截图 文字说明

从应用层 常用协议或 自定义协议 传输层 TCP 段 网络层

IP 包 链路层 以太帧 物理层 比特 解释一次聊天消

息的完整传输过程。

关于 自定义协议

不需要设计得像 HTTP 那么复杂，只要能解决两个基本问题：

对方知道发来的是什么类型的数据 是普通聊天消息 还是文件传输

请求，还是退出指令？）

对方知道数据从哪里开始、到哪里结束（解决 TCP 流式传输的 粘

包 问题）

拓展要求（选做，加分

支持群聊（服务器转发） 私聊。

客户端支持文件传输（混合 TCP UDP

增加历史聊天记录存储（本地 SQLite 或文件

题目二： AI 驱动的网络流量分类器（难度中等偏高）

目标：

开发一个网络嗅探器 基于原始套接字或 pcap 库 捕获网络数据包

提取五层头部关键字段作为特征 训练一个简单的机器学习模型 如

决策树 或调用云端分类 API 实时判断流量类型 如 Web P2P VoIP

游戏等

## 第 3 页

核心要求（必做

抓包模块 使用 scapy Python 或 pcap4J Java 捕获实时数据包

或读取 pcap 文件。

特征提取 从每一条流 五元组 中提取特征 例如 平均包长 包

到达间隔方差、传输层协议、端口号、 TTL 值等。

分类模块：建议 用开源数据集（如 UNSW-NB15 ISCX VPN-nonVPN

训练一个简单分类器（决策树 /KNN ，保存模型并在程序中加载。

实时显示 程序运行时可选择网卡 抓取 100 个包后输出 当前流量

类别及置信度

另： 报告 中要求详细写出 一个 HTTP 请求包从浏览器产生到被你的

程序捕获的过程中，经过五层时分别添加 删除了哪些头部信息

拓展要求（选做，加分

实时图形化界面（显示流量类别随时间的变化柱状图

检测常见攻击流量（如 SYN flood 、端口扫描 ，并报警。

自己采集一小批真实校园网流量并标注，微调模型。
